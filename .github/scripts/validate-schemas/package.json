{
  "name": "test-ajv",
  "version": "1.0.0",
  "description": "JSON Schema validation for test-ajv",
  "scripts": {
    "validate": "cross-env SCHEMA_PATH=\"../../../**/*.schema.json\" node validate-schemas.js",
    "validate-tests": "cross-env SCHEMA_PATH=\"tests/**/*.schema.json\" node validate-schemas.js",
    "test": "node test-validation.js"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1",
    "ajv-keywords": "^5.1.0",
    "glob": "^10.3.10"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "eslint": "^10.8.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
